# Financial Modeling Prep API key — profile, financials, estimates, peers, etc.
FMP_API_KEY=
# FRED (St. Louis Fed) API key — macro series
FRED_API_KEY=
# OpenAI API key — embeddings for ingest/search
OPENAI_API_KEY=
# Perplexity API key — optional; only needed for `prefetch --kinds perplexity`
PERPLEXITY_API_KEY=
